<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <!-- Update SMS queue processing frequency -->
    <record id="sms_ir_cron_sms_queue" model="ir.cron">
        <field name="name">Process SMS Queue (MTech)</field>
        <field name="model_id" ref="model_sms_sms"/>
        <field name="state">code</field>
        <field name="code">model._process_queue()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">minutes</field>
        <field name="active">True</field>
        <field name="user_id" ref="base.user_root"/>
    </record>
</odoo>